<% content_for :functional_nav do%>
<li><%= link_to 'Add', new_genre_path %></li>
<% end %>
<%= currently_at 'Genres'%>

<table id="genres" class="display">
<thead>	
  <tr>
    <th>Id</th>  	
    <th>Name</th>


  </tr>
</thead>
<tbody>
<% @genres.each do |genre| %>
  <tr>
    <td><%= link_to(genre.id, genre) %></td>  	
    <td><%= genre.name %></td>
    	
  </tr>
<% end %>
</tbody>
</table>

<br />

<%= link_to 'New Genre', new_genre_path %>
