<%= form_for(@lyric) do |f| %>
  <% if @lyric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lyric.errors.count, "error") %> prohibited this lyric from being saved:</h2>

      <ul>
      <% @lyric.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :song_name %><br />
    <%= f.collection_select :song_id, Song.all, :id, :name %>

  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, cols: 40, rows: 20 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
